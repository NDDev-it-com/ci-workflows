[project]
name = "ciwf-fixture"
version = "0.1.0"
description = "Smallest installable package that proves python-ci runs."
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ciwf_fixture"]
